{
  "name": "node-starter",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@sentry/esbuild-plugin": "2.2.2",
    "dotenv": "^16.1.4",
    "esbuild": "^0.18.2"
  },
  "dependencies": {
    "@sentry/node": "^7.55.2"
  }
}
